import React, { useRef } from "react";

import { IonContent, IonPage } from "@ionic/react";

const TodayScheduleHome: React.FC = () => {
  return (
    <IonPage>
      <IonContent>Today's Schedule</IonContent>
    </IonPage>
  );
};

export default TodayScheduleHome;
