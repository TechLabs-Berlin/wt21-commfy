import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.techlabs.bl.wt21.commfy',
  appName: 'ionic-app-base',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
